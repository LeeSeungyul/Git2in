#!/usr/bin/env python3
"""Git2in CLI executable."""

import sys
from pathlib import Path

# Add project root to path for imports
project_root = Path(__file__).parent
sys.path.insert(0, str(project_root))

from src.cli.main import app

if __name__ == "__main__":
    app()